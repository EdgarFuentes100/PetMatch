@{
    ViewData["Title"] = "Acceso denegado";
}
<div class="text-center mt-5">
    <h2 class="text-danger">🚫 No tienes permisos para ver esta página.</h2>
    @if (ViewData["ReturnUrl"] is string url && !string.IsNullOrEmpty(url))
    {
        <p class="mt-3">
            <a class="btn btn-secondary" href="@url">Volver</a>
        </p>
    }
</div>
